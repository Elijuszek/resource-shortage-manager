using System.ComponentModel;

namespace ResourceShortageManager.Models
{
    public enum Room
    {
        MeetingRoom,
        Bathroom,
        Kitchen
    }

    public enum Category
    {
        Electronics,
        Other,
        Food
    }
}
